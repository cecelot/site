---
const { title, description, url, post } = Astro.props;
---

<div class="flex flex-col">
  <a href={url}>{title}</a>
  <span class="text-subtext1">{description}</span>
  <span class="text-subtext0">Learn more: <a href={post}>{post}</a></span>
</div>
