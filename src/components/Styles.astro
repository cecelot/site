<style is:global>
  a:not(.ignored-link) {
    @apply relative text-blue hover:underline;
  }

  a[href*="//"]:not(.ignored-link)
  {
    @apply pr-5;
  }

  a[href*="//"]:not(.ignored-link)::after
  {
    content: " ";
    -webkit-mask-image: url("external-link.svg");
    mask-image: url("/external-link.svg");
    @apply absolute mt-[3px] ml-1 inline-block bg-blue h-full w-4 [mask-repeat:no-repeat] [mask-size:contain];
  }

  article a[href*="//"]:not(.ignored-link)::after
  {
    @apply lg:mt-2;
  }

  footer a[href*="//"]:not(.ignored-link)::after
  {
    @apply mt-[5px];
  }

  pre {
    @apply text-wrap;
  }

  .footnotes {
    @apply border-t-2 border-crust;
  }

  .astro-code {
    @apply border-2 border-crust rounded-xl;
  }

  @media (prefers-color-scheme: dark) {
    .astro-code,
    .astro-code span {
      color: var(--shiki-dark) !important;
      background-color: var(--shiki-dark-bg) !important;
      font-style: var(--shiki-dark-font-style) !important;
      font-weight: var(--shiki-dark-font-weight) !important;
      text-decoration: var(--shiki-dark-text-decoration) !important;
    }
  }
</style>
