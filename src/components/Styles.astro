<style is:global>
  a:not(.ignored-link) {
    @apply text-blue hover:underline transition-all;
  }

  a[href*="//"]:not(.ignored-link)::after
  {
    content: " ";
    -webkit-mask-image: url("external-link.svg");
    mask-image: url("/external-link.svg");
    @apply inline-block bg-blue h-3 w-4 [mask-repeat:no-repeat] [mask-size:contain] ml-[3px];
  }

  svg {
    @apply fill-blue stroke-blue text-blue;
  }

  .astro-code {
    @apply border-2 border-crust rounded-xl;
  }

  @media (prefers-color-scheme: dark) {
    .astro-code,
    .astro-code span {
      color: var(--shiki-dark) !important;
      background-color: var(--shiki-dark-bg) !important;
      font-style: var(--shiki-dark-font-style) !important;
      font-weight: var(--shiki-dark-font-weight) !important;
      text-decoration: var(--shiki-dark-text-decoration) !important;
    }
  }
</style>
